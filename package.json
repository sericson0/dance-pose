{
  "name": "tangle",
  "version": "0.1.0",
  "description": "TAngle — interactive 3D tool for posing tango dancers with anatomical joint limits, center-of-gravity analysis, and pose comparison",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "bake:rig": "node scripts/build-calibration.mjs",
    "test": "node --test",
    "lint": "eslint src scripts"
  },
  "dependencies": {
    "three": "^0.166.0"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "globals": "^17.7.0",
    "puppeteer-core": "^25.3.0",
    "vite": "^8.1.3"
  }
}
